{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "#!/bin/bash\nif [ \"$CLAUDE_CODE_REMOTE\" = \"true\" ]; then\n  pip install -r requirements.txt\n  cd web-frontend && npm install\nfi\nexit 0"
          }
        ]
      }
    ]
  }
}
